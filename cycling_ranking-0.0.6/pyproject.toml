[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.render]
build-command = "pip install -r requirements.txt"
start-command = "gunicorn --bind $PORT --workers 1 app:app"
